@model LaserArt.Models.Product

@{
    ViewBag.Title = Model.ProductTitle;
}
<div class="jumbotron">
    <h2 class="h1">@Model.ProductTitle</h2>
    @if (this.User.IsInRole("Administrator"))
    {
        <a id="@Model.Id" class="deleteProduct">Ջնջել</a>
    }
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <img src="@Model.ImageSource" class="imageHeader" id="generalImg" />
            </div>
            <div class="col-md-6">
                <hr class="productPagehr">

                <p> <h class="productpageh3">Գինը:</h>@Model.Price դրամ.</p>
                <hr class="productPagehr">
                @*<input type="number" id="cardQuantity" value="1" class="form-control" />*@
                @*<div style="color:red" id="cardQuantityRequired"></div>
                <input type="button" value="Добавить в корзину +" class="btn btn-default" onclick="addToCard(@Model.Id)" />*@
                <h2 class="productpageh3">Նկարագրություն</h2>
                @Html.Raw(Model.ProductDescription)
                <hr class="productPagehr">
            </div>
        </div>
    </div>
</div>
