@model  List<LaserArt.Models.Category>

@{
    ViewBag.Title = "Главная";
}

<div class="jumbotron" style="padding:0 !important;">
    <div class="row" style="background-color:transparent !important;">
        @if (!Request.Browser.IsMobileDevice)
        {

            @*<div class="col-md-3">
                <img src="~/images/logo_croissant.png" class="mainlogo" />
            </div>*@
            <div class="col-md-9">

                @*<div class="w3-content w3-section">
                    @foreach (var item in ViewBag.Sales)
                    {
                        <a class="mySlides" href="@item.link">
                            <img class="myslideImage" src="@item.image">
                        </a>
                    }

                </div>*@
            </div>
        }
        else
        {
            @*<div class="col-md-12">
                <img src="~/images/logo_croissant.png" class="mainlogo" />
            </div>*@
        }
    </div>
</div>
<div class="container jumbotron" style="padding-top:5px !important;">
    <div class="row" align="left">
        @ViewBag.Info
    </div>

   
        @{int i = 0; }
        @foreach (var category in Model)
        {

            if (i == 0 || i % 3 == 0)
            {
                @Html.Raw("<div class=\"row\" >");
            }
            <div id="@category.Id" class="col-md-4 item-div" align="center">
                <img style="width:100%; height:auto;" src="@category.CategoryImage" />
                <p>@category.CategoryName</p>
            </div>


         
            i++;
            if (i != 0 && i % 3 == 0 || i == Model.Count)
            {
                @Html.Raw("</div>")
            }
        }
       
</div>
@*<script src="~/Scripts/carousel.js"></script>*@